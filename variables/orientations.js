import { entries } from '../functions/toolbox.js'

export const directions = entries({
  a: '',
  t: '-top',
  r: '-right',
  b: '-bottom',
  l: '-left',
})

export const positions = ['top', 'right', 'bottom', 'left', 'center']
